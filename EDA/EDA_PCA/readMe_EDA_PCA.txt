EDA.py: This file reads in the labeled data 
and performs EDA related to dimensionality
reduction (we used PCA, we tried t-SNE but we were
running into some problems with it). The script
does not save the results anywhere, it just displays
the graphs one by one when the script is run.